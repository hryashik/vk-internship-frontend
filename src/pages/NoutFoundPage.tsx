const NotFoundPage = () => {
   return (
      <div
         style={{
            display: "flex",
            alignItems: "center",
            justifyContent: "center",
            height: "100vh",
         }}
      >
         <h4>Page was not found ;(</h4>
      </div>
   );
};

export default NotFoundPage;
